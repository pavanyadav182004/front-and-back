spring.application.name=Hotel_Booking
spring.config.import=optional:file:./application-local.properties,optional:file:${user.dir}/application-local.properties,optional:file:./Hotel_Booking/application-local.properties,optional:file:${user.dir}/Hotel_Booking/application-local.properties


spring.datasource.url=jdbc:mysql://localhost:3306/Hotel_Booking

spring.datasource.username=root
spring.datasource.password=root
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

jwt.secret=mysecretkeymysecretkeymysecretkey123456
jwt.expiration=86400000000000


spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

server.port=8082

# Email OTP settings. For Gmail, use an app password, not your normal password.
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay settings. Replace with your actual keys from Razorpay dashboard.
razorpay.key=rzp_test_SiQKyz5hKmsXlp
razorpay.secret=hNY2faFBtpgObJyx5I9mszgI

