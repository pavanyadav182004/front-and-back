spring.application.name=Hotel_Booking
# spring.config.import=optional:file:./application-local.properties

# Datasource Connection (Prioritizes Railway/Render env variables: MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, SPRING_DATASOURCE_URL)
spring.datasource.url=${SPRING_DATASOURCE_URL:${MYSQL_URL:jdbc:mysql://${MYSQLHOST:gateway01.ap-southeast-1.prod.aws.tidbcloud.com}:${MYSQLPORT:4000}/${MYSQLDATABASE:test}?useSSL=true&enabledTLSProtocols=TLSv1.2,TLSv1.3}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${MYSQLUSER:3jRFguataWyaew5.root}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${MYSQLPASSWORD:6ne83GMVs9GQMuEt}}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ddl-auto=update ensures database tables and data are preserved forever on restarts
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

jwt.secret=mysecretkeymysecretkeymysecretkey123456
jwt.expiration=86400000000000


spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

server.port=${PORT:8080}

# Email OTP settings. For Gmail, use an app password, not your normal password.
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay settings. Replace with your actual keys from Razorpay dashboard.
razorpay.key=rzp_test_SiQKyz5hKmsXlp
razorpay.secret=hNY2faFBtpgObJyx5I9mszgI

server.tomcat.max-http-form-post-size=50000000
spring.servlet.multipart.max-file-size=5000MB
spring.servlet.multipart.max-request-size=5000MB